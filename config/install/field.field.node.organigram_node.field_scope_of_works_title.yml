langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_scope_of_works_title
    - node.type.organigram_node
id: node.organigram_node.field_scope_of_works_title
field_name: field_scope_of_works_title
entity_type: node
bundle: organigram_node
label: "Scope of work's title"
description: 'Add a custom title for the “Scope of work” section if you need to override the default section title. Otherwise, leave this field empty.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string
